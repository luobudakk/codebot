# 示例知识片段（Phase A 验收）

这是一条普通段落，用于验证「语义分块」会按长度与段落合并。

下面是一段应被整体保留的代码块：

```python
def fib(n: int) -> int:
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

文末公式（若后续接入 LaTeX 分块策略可扩展）：行内示例 \(E = mc^2\)。
